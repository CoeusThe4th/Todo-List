<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <title></title>
        <meta name="description" content="">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="">
    </head>
    <body>

        <h1>Todo List Now!</h1>
        <!-- Rendering of Todo list items from database -->        
        <ul class="todo">
            <% for(let i = 0; i < info.length; i++) {%>
                <li class="todos">
                    <span><%= info[i].itemName %></span>

                <!-- Button for deleting an item -->
                    <button class="del" data-stage="<%= info[i].itemName %>">Delete</button>
                </li>
            <% } %>
        </ul>

        <h2>Add a new item</h2>
        <!-- Form for adding a new item to list -->
        <form action="/addItem" method="post">
            <label for="ItemName">Item Name:</label>
            <input type="text" id="itemName" name="itemName" required>
            <button type="submit">Add item</button>
        </form>

        <script src="main.js" type="text/javascript" async defer></script>
    </body>
</html>